# ─── Frontend ──────────────────────────────────────────────────────────────────
# The URL of the API server. Must match the API port and prefix.
NEXT_PUBLIC_API_URL=http://localhost:3001/api
# The Stellar network to use in the frontend (testnet or public)
NEXT_PUBLIC_STELLAR_NETWORK=testnet
